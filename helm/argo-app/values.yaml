# Default values for argo-app.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

environment: qa
image_tag: 3.0-alpha-20210303-173025
app_name: spring-rest-demo
serverless: "True"

pipeline:
  enabled: false
  namespace: att-pipelines
