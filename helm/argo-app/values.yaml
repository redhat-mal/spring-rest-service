# Default values for argo-app.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

environment: qa
image_tag: 3.0-alpha-20210303-165156
app_name: spring-rest-demo
serverless: true

pipeline:
  enabled: false
  namespace: att-pipelines
